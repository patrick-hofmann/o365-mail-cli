# Read-only profile: can only read emails, folders, rules, and config.
# No sending, modifying, moving, or deleting.
description: "Read-only access"
enforce: false
allow:
  - mail.read
  - folders.read
  - rules.read
  - config.read
  - auth
